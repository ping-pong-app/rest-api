{
    "title": "Firebase Cloud Messaging message schema definition for PING action",
    "type": "object",
    "properties": {
        "userId": {
            "type": "string",
            "description": "Firebase Authentication user id (UID)"
        },
        "groupId": {
            "type": "string",
            "description": "Pinged group's id (UID)"
        },
        "timestamp": {
            "type": "string",
            "description": "Timestamp in ISO format"
        }
    },
    "required": [
        "groupId",
        "timestamp",
        "userId"
    ]
}
